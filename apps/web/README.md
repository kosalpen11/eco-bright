# apps/web

The storefront currently runs from the repository root while the monorepo
packages and Telegram bot are being introduced. This folder is the target
location for a later extraction once the shared package boundaries have fully
replaced root-local imports.
