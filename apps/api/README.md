# apps/api

The current API surface still lives in the Next.js Route Handlers inside the
storefront. This folder is reserved for a future dedicated API service once the
shared `packages/db`, `packages/order-core`, and `packages/catalog-core`
contracts are stable.
